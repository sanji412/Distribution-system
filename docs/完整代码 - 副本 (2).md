```properties
spring.application.name=stock-service

# Service listening port
server.port=8003

# ========== Database Configuration ==========
# MySQL database driver class
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JDBC connection URL for stock database
spring.datasource.url=jdbc:mysql://localhost:3306/stock_db?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=GMT%2B8

# Database login username
spring.datasource.username=root

# Database login password
spring.datasource.password=root

# Max active connections in connection pool
spring.datasource.hikari.maximum-pool-size=10

# Connection wait timeout (milliseconds)
spring.datasource.hikari.connection-timeout=30000

# ========== MyBatis-Plus Configuration ==========
# Mapper XML file scan path
mybatis-plus.mapper-locations=classpath:mapper/**/*.xml

# Entity package for type alias
mybatis-plus.type-aliases-package=com.buu.stock.entity

# Auto map underscore column to camel case field
mybatis-plus.configuration.map-underscore-to-camel-case=true

# Disable second-level cache
mybatis-plus.configuration.cache-enabled=false

# Database primary key auto increment
mybatis-plus.global-config.db-config.id-type=AUTO

# ========== Business Configuration ==========
# Safe stock threshold, trigger warning when stock is lower
stock.safe.threshold=20

# Retry times when stock deduction fails
stock.deduct.retry.times=3

# Enable stock operation log record
stock.operation.log.enable=true

# ========== Log Configuration ==========
# Mapper layer log level (debug for SQL print)
logging.level.com.buu.stock.mapper=debug

# Root log level
logging.level.root=info

# Log file output path
logging.file.name=./logs/stock-service.log
```

# 一、用户服务user-service

## 1.依赖

~~~xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.buu</groupId>
        <artifactId>inventory-parent</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>user-service</artifactId>
    <packaging>jar</packaging>
    <name>user-service</name>
    <description>用户服务</description>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-crypto</artifactId>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
~~~



## 2.配置

~~~properties
spring.application.name=user-service
# Server port for user service (user service listening port)
server.port=8081

# Database connection URL for user data storage
spring.datasource.url=jdbc:mysql://localhost:3306/user_db?useSSL=false&serverTimezone=UTC

# Database username
spring.datasource.username=root

# Database password
spring.datasource.password=root

# Maximum number of active database connections
spring.datasource.hikari.maximum-pool-size=10

# Timeout for user session (in seconds)
user.session.timeout=1800

# Enable user password encryption (true/false)
user.password.encryption.enabled=true
~~~



## 3.代码

### 1. 启动类

```java
package com.buu.userservice;

import org.mybatis.spring.annotation.MapperScan;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
@MapperScan("com.buu.userservice.mapper")
public class UserServiceApplication {

    public static void main(String[] args) {
        SpringApplication.run(UserServiceApplication.class, args);
    }

}

```

### 2. 实体类

```java
package com.buu.userservice.entity;

import com.baomidou.mybatisplus.annotation.IdType;
import com.baomidou.mybatisplus.annotation.TableId;
import com.baomidou.mybatisplus.annotation.TableName;
import lombok.Data;
import java.time.LocalDateTime;

/**
 * 系统用户实体
 * 对应 user_db.sys_user 表
 */
@Data
@TableName("sys_user")
public class SysUser {
    /** 用户主键ID */
    @TableId(type = IdType.AUTO)
    private Long userId;
    /** 登录用户名 */
    private String username;
    /** 登录密码（BCrypt加密） */
    private String password;
    /** 用户昵称 */
    private String nickname;
    /** 用户角色：admin-管理员、user-普通用户 */
    private String role;
    /** 账号状态：0-禁用、1-启用 */
    private Integer status;
    /** 联系电话 */
    private String phone;
    /** 创建时间 */
    private LocalDateTime createTime;
    /** 更新时间 */
    private LocalDateTime updateTime;
}
```

### 3. Mapper 接口

```java

```

### 4. Mapper XML

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.buu.user.mapper.SysUserMapper">
    <!-- 复杂SQL在此处编写，基础CRUD由MyBatis-Plus提供 -->
</mapper>
```

### 5. Service 接口

```java

```

### 6. Service 实现类

```java

```

### 7. Controller 控制器

```java

```

### 8.统一返回

~~~java
package com.buu.userservice.common;

import lombok.Data;

/**
 * 统一接口返回结果
 * 封装所有接口的响应格式，保证前后端交互规范统一
 * @param <T> 响应数据泛型
 */
@Data
public class R<T> {
    /** 响应状态码：200成功，500失败 */
    private Integer code;
    /** 响应提示信息 */
    private String msg;
    /** 响应业务数据 */
    private T data;

    /**
     * 成功响应（携带数据）
     * @param data 接口返回数据
     * @return 统一响应对象
     */
    public static <T> R<T> success(T data) {
        R<T> result = new R<>();
        result.setCode(200);
        result.setMsg("操作成功");
        result.setData(data);
        return result;
    }

    /**
     * 成功响应（无数据）
     * @return 统一响应对象
     */
    public static <T> R<T> success() {
        return success(null);
    }

    /**
     * 失败响应
     * @param message 错误描述信息
     * @return 统一响应对象
     */
    public static <T> R<T> fail(String message) {
        R<T> result = new R<>();
        result.setCode(500);
        result.setMsg(message);
        return result;
    }

    /**
     * 失败响应（自定义状态码）
     * @param code 错误状态码
     * @param message 错误描述信息
     * @return 统一响应对象
     */
    public static <T> R<T> fail(Integer code, String message) {
        R<T> result = new R<>();
        result.setCode(code);
        result.setMsg(message);
        return result;
    }
}

~~~



# 二、商品服务product-service

## 1.依赖

~~~xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
		 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>com.buu</groupId>
		<artifactId>inventory-parent</artifactId>
		<version>1.0.0</version>
	</parent>

	<artifactId>product-service</artifactId>
	<packaging>jar</packaging>
	<name>product-service</name>
	<description>商品服务</description>

	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<dependency>
			<groupId>com.baomidou</groupId>
			<artifactId>mybatis-plus-boot-starter</artifactId>
		</dependency>
		<dependency>
			<groupId>com.mysql</groupId>
			<artifactId>mysql-connector-j</artifactId>
		</dependency>

		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>
</project>
~~~



## 2.配置

~~~properties
spring.application.name=product-service
# Server port for product service
server.port=8084

# Database connection URL for product data storage
spring.datasource.url=jdbc:mysql://localhost:3306/product_db?useSSL=false&serverTimezone=UTC

# Database username
spring.datasource.username=root

# Database password
spring.datasource.password=root

# Product stock warning threshold (alert when stock is below this value)
product.stock.warning.threshold=10

# Product image storage path
product.image.storage.path=/opt/app/product/images

# Product cache expiration time (in minutes)
product.cache.expire.minutes=30
~~~



## 3.代码

### 1. 启动类

```java
package com.buu.productservice;

import org.mybatis.spring.annotation.MapperScan;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * 商品服务启动类
 * 提供商品档案、浏览历史、分类管理等能力
 */
@SpringBootApplication
@MapperScan("com.buu.productservice.mapper")
public class ProductServiceApplication {
	public static void main(String[] args) {
		SpringApplication.run(ProductServiceApplication.class, args);
	}
}
```

### 2. 实体类

**Product.java**

```java
package com.buu.productservice.entity;

import com.baomidou.mybatisplus.annotation.IdType;
import com.baomidou.mybatisplus.annotation.TableId;
import com.baomidou.mybatisplus.annotation.TableName;
import lombok.Data;
import java.math.BigDecimal;
import java.time.LocalDateTime;

/**
 * 商品实体
 * 对应 product_db.product 表
 */
@Data
@TableName("product")
public class Product {
    /** 商品主键ID */
    @TableId(type = IdType.AUTO)
    private Long productId;
    /** 商品名称 */
    private String productName;
    /** 商品分类 */
    private String category;
    /** 商品单价 */
    private BigDecimal price;
    /** 商品SKU编码 */
    private String skuCode;
    /** 商品描述 */
    private String description;
    /** 上下架状态：0-下架、1-上架 */
    private Integer status;
    /** 安全库存阈值 */
    private Integer safeStock;
    /** 创建时间 */
    private LocalDateTime createTime;
    /** 更新时间 */
    private LocalDateTime updateTime;
}
```

**BrowseHistory.java**

```java
package com.buu.productservice.entity;

import com.baomidou.mybatisplus.annotation.IdType;
import com.baomidou.mybatisplus.annotation.TableId;
import com.baomidou.mybatisplus.annotation.TableName;
import lombok.Data;
import java.time.LocalDateTime;

/**
 * 用户浏览历史实体
 * 对应 product_db.browse_history 表
 */
@Data
@TableName("browse_history")
public class BrowseHistory {
    /** 主键ID */
    @TableId(type = IdType.AUTO)
    private Long id;
    /** 浏览用户ID */
    private Long userId;
    /** 被浏览商品ID */
    private Long productId;
    /** 浏览时间 */
    private LocalDateTime browseTime;
}
```

### 3. Mapper 接口

**ProductMapper.java**

```java

```

**BrowseHistoryMapper.java**

```java

```

### 4. Mapper XML

~~~xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.buu.productservice.mapper.BrowseHistoryMapper">
    <!-- 复杂SQL在此处编写，基础CRUD由MyBatis-Plus提供 -->
</mapper>
~~~

~~~xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.buu.productservice.mapper.ProductMapper">
    <!-- 复杂SQL在此处编写，基础CRUD由MyBatis-Plus提供 -->
</mapper>
~~~



### 5. Service 接口

**ProductService.java**

```java

```

**BrowseHistoryService.java**

```java

```

### 6. Service 实现类

**ProductServiceImpl.java**

```java

```

**BrowseHistoryServiceImpl.java**

```java

```

### 7. Controller

```java

```

### 8. 统一返回

~~~java
package com.buu.productservice.common;

import lombok.Data;

/**
 * 统一接口返回结果
 * 封装所有接口的响应格式，保证前后端交互规范统一
 * @param <T> 响应数据泛型
 */
@Data
public class R<T> {
    /** 响应状态码：200成功，500失败 */
    private Integer code;
    /** 响应提示信息 */
    private String msg;
    /** 响应业务数据 */
    private T data;

    /**
     * 成功响应（携带数据）
     * @param data 接口返回数据
     * @return 统一响应对象
     */
    public static <T> R<T> success(T data) {
        R<T> result = new R<>();
        result.setCode(200);
        result.setMsg("操作成功");
        result.setData(data);
        return result;
    }

    /**
     * 成功响应（无数据）
     * @return 统一响应对象
     */
    public static <T> R<T> success() {
        return success(null);
    }

    /**
     * 失败响应
     * @param message 错误描述信息
     * @return 统一响应对象
     */
    public static <T> R<T> fail(String message) {
        R<T> result = new R<>();
        result.setCode(500);
        result.setMsg(message);
        return result;
    }

    /**
     * 失败响应（自定义状态码）
     * @param code 错误状态码
     * @param message 错误描述信息
     * @return 统一响应对象
     */
    public static <T> R<T> fail(Integer code, String message) {
        R<T> result = new R<>();
        result.setCode(code);
        result.setMsg(message);
        return result;
    }
}
~~~



# 三、库存服务stock-service

## 1.依赖

~~~xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.buu</groupId>
        <artifactId>inventory-parent</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>stock-service</artifactId>
    <packaging>jar</packaging>
    <name>stock-service</name>
    <description>库存服务</description>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
~~~



## 2.配置

~~~properties
spring.application.name=stock-service

# Service listening port
server.port=8003

# ========== Database Configuration ==========
# MySQL database driver class
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JDBC connection URL for stock database
spring.datasource.url=jdbc:mysql://localhost:3306/stock_db?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=GMT%2B8

# Database login username
spring.datasource.username=root

# Database login password
spring.datasource.password=root

# Max active connections in connection pool
spring.datasource.hikari.maximum-pool-size=10

# Connection wait timeout (milliseconds)
spring.datasource.hikari.connection-timeout=30000

# ========== MyBatis-Plus Configuration ==========
# Mapper XML file scan path
mybatis-plus.mapper-locations=classpath:mapper/**/*.xml

# Entity package for type alias
mybatis-plus.type-aliases-package=com.buu.stock.entity

# Auto map underscore column to camel case field
mybatis-plus.configuration.map-underscore-to-camel-case=true

# Disable second-level cache
mybatis-plus.configuration.cache-enabled=false

# Database primary key auto increment
mybatis-plus.global-config.db-config.id-type=AUTO

# ========== Business Configuration ==========
# Safe stock threshold, trigger warning when stock is lower
stock.safe.threshold=20

# Retry times when stock deduction fails
stock.deduct.retry.times=3

# Enable stock operation log record
stock.operation.log.enable=true

# ========== Log Configuration ==========
# Mapper layer log level (debug for SQL print)
logging.level.com.buu.stock.mapper=debug

# Root log level
logging.level.root=info

# Log file output path
logging.file.name=./logs/stock-service.log
~~~



## 3.代码

### 1. 启动类

```java
package com.buu.stockservice;

import org.mybatis.spring.annotation.MapperScan;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
@MapperScan("com.buu.stockservice.mapper")
public class StockServiceApplication {

    public static void main(String[] args) {
        SpringApplication.run(StockServiceApplication.class, args);
    }

}

```

### 2. 实体类

**Warehouse.java**

```java
package com.buu.stockservice.entity;

import com.baomidou.mybatisplus.annotation.IdType;
import com.baomidou.mybatisplus.annotation.TableId;
import com.baomidou.mybatisplus.annotation.TableName;
import lombok.Data;
import java.time.LocalDateTime;

/**
 * 仓库实体
 * 对应 stock_db.warehouse 表
 */
@Data
@TableName("warehouse")
public class Warehouse {
    /** 仓库主键ID */
    @TableId(type = IdType.AUTO)
    private Long warehouseId;
    /** 仓库名称 */
    private String warehouseName;
    /** 仓库地址 */
    private String warehouseAddress;
    /** 状态：0-停用、1-启用 */
    private Integer status;
    /** 创建时间 */
    private LocalDateTime createTime;
}
```

**Stock.java**

```java
package com.buu.stockservice.entity;

import com.baomidou.mybatisplus.annotation.IdType;
import com.baomidou.mybatisplus.annotation.TableId;
import com.baomidou.mybatisplus.annotation.TableName;
import lombok.Data;
import java.time.LocalDateTime;

/**
 * 库存实体
 * 对应 stock_db.stock 表
 */
@Data
@TableName("stock")
public class Stock {
    /** 库存记录ID */
    @TableId(type = IdType.AUTO)
    private Long stockId;
    /** 关联商品ID */
    private Long productId;
    /** 所属仓库ID */
    private Long warehouseId;
    /** 当前可用库存数量 */
    private Integer stockNum;
    /** 创建时间 */
    private LocalDateTime createTime;
    /** 更新时间 */
    private LocalDateTime updateTime;
}
```

### 3. Mapper 接口

**WarehouseMapper.java**



```java

```

**StockMapper.java**



```java

```

### 4. StockMapper.xml



```xml

```

### 5. Service 接口

**WarehouseService.java**

```java

```

**StockService.java**

```java

```

### 6. Service 实现类

**WarehouseServiceImpl.java**



```java

```

**StockServiceImpl.java**



```java

```

### 7. Controller

```java

```

### 8.统一返回

~~~java
package com.buu.stockservice.common;

import lombok.Data;

/**
 * 统一接口返回结果
 * 封装所有接口的响应格式，保证前后端交互规范统一
 * @param <T> 响应数据泛型
 */
@Data
public class R<T> {
    /** 响应状态码：200成功，500失败 */
    private Integer code;
    /** 响应提示信息 */
    private String msg;
    /** 响应业务数据 */
    private T data;

    /**
     * 成功响应（携带数据）
     * @param data 接口返回数据
     * @return 统一响应对象
     */
    public static <T> R<T> success(T data) {
        R<T> result = new R<>();
        result.setCode(200);
        result.setMsg("操作成功");
        result.setData(data);
        return result;
    }

    /**
     * 成功响应（无数据）
     * @return 统一响应对象
     */
    public static <T> R<T> success() {
        return success(null);
    }

    /**
     * 失败响应
     * @param message 错误描述信息
     * @return 统一响应对象
     */
    public static <T> R<T> fail(String message) {
        R<T> result = new R<>();
        result.setCode(500);
        result.setMsg(message);
        return result;
    }

    /**
     * 失败响应（自定义状态码）
     * @param code 错误状态码
     * @param message 错误描述信息
     * @return 统一响应对象
     */
    public static <T> R<T> fail(Integer code, String message) {
        R<T> result = new R<>();
        result.setCode(code);
        result.setMsg(message);
        return result;
    }
}
~~~



# 四、订单和配送服务order-service

## 1.依赖

~~~xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.buu</groupId>
        <artifactId>inventory-parent</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>order-service</artifactId>
    <packaging>jar</packaging>
    <name>order-service</name>
    <description>订单与配送服务</description>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
~~~



## 2.配置

~~~properties
spring.application.name=order-service

# Server port for order service
server.port=8082

# Database connection URL for order data storage
spring.datasource.url=jdbc:mysql://localhost:3306/order_db?useSSL=false&serverTimezone=UTC

# Database username
spring.datasource.username=root

# Database password
spring.datasource.password=root

# Order timeout duration (in minutes, auto-cancel overdue orders)
order.timeout.minutes=30

# Enable order cache (reduce database query pressure)
order.cache.enabled=true

# Order cache expiration time (in seconds)
order.cache.expire.seconds=600
~~~



## 3.代码

### 1. 启动类



```java
package com.buu.orderservice;

import org.mybatis.spring.annotation.MapperScan;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
@MapperScan("com.buu.orderservice.mapper")
public class OrderServiceApplication {

    public static void main(String[] args) {
        SpringApplication.run(OrderServiceApplication.class, args);
    }

}

```

### 2. 实体类

**OrderMain.java**



```java
package com.buu.orderservice.entity;

import com.baomidou.mybatisplus.annotation.IdType;
import com.baomidou.mybatisplus.annotation.TableId;
import com.baomidou.mybatisplus.annotation.TableName;
import lombok.Data;
import java.math.BigDecimal;
import java.time.LocalDateTime;

/**
 * 订单主表实体
 * 对应 order_db.order_main 表
 */
@Data
@TableName("order_main")
public class OrderMain {
    /** 订单主键ID */
    @TableId(type = IdType.AUTO)
    private Long orderId;
    /** 订单编号 */
    private String orderNo;
    /** 下单用户ID */
    private Long userId;
    /** 商品名称（冗余） */
    private String productName;
    /** 购买数量（冗余） */
    private Integer productNum;
    /** 订单总金额 */
    private BigDecimal totalAmount;
    /** 订单状态：待支付、已支付、已发货、已完成、库存不足 */
    private String orderStatus;
    /** 收货人姓名 */
    private String receiverName;
    /** 收货人电话 */
    private String receiverPhone;
    /** 收货地址 */
    private String receiverAddress;
    /** 物流公司 */
    private String logisticsCompany;
    /** 物流单号 */
    private String logisticsNo;
    /** 物流状态：待发货、运输中、已签收 */
    private String logisticsStatus;
    /** 当前物流位置 */
    private String currentLocation;
    /** 预计送达时间 */
    private LocalDateTime expectArriveTime;
    /** 下单时间 */
    private LocalDateTime createTime;
    /** 支付时间 */
    private LocalDateTime payTime;
    /** 发货时间 */
    private LocalDateTime deliveryTime;
    /** 订单完成时间 */
    private LocalDateTime finishTime;
}
```

**OrderItem.java**



```java
package com.buu.orderservice.entity;

import com.baomidou.mybatisplus.annotation.IdType;
import com.baomidou.mybatisplus.annotation.TableId;
import com.baomidou.mybatisplus.annotation.TableName;
import lombok.Data;
import java.math.BigDecimal;
import java.time.LocalDateTime;

/**
 * 订单明细表实体
 * 对应 order_db.order_item 表
 */
@Data
@TableName("order_item")
public class OrderItem {
    /** 明细主键ID */
    @TableId(type = IdType.AUTO)
    private Long itemId;
    /** 关联订单主表ID */
    private Long orderId;
    /** 商品ID */
    private Long productId;
    /** 商品名称（冗余） */
    private String productName;
    /** 商品单价（冗余） */
    private BigDecimal productPrice;
    /** 购买数量 */
    private Integer quantity;
    /** 明细小计金额 */
    private BigDecimal subTotal;
    /** 创建时间 */
    private LocalDateTime createTime;
}
```

**AiChatRecord.java**



```java
package com.buu.orderservice.entity;

import com.baomidou.mybatisplus.annotation.IdType;
import com.baomidou.mybatisplus.annotation.TableId;
import com.baomidou.mybatisplus.annotation.TableName;
import lombok.Data;
import java.time.LocalDateTime;

/**
 * AI对话记录实体
 * 对应 order_db.ai_chat_record 表
 */
@Data
@TableName("ai_chat_record")
public class AiChatRecord {
    /** 对话记录ID */
    @TableId(type = IdType.AUTO)
    private Long chatId;
    /** 提问用户ID */
    private Long userId;
    /** 用户提问内容 */
    private String question;
    /** AI回复内容 */
    private String answer;
    /** 对话时间 */
    private LocalDateTime createTime;
}
```

### 3. Mapper 接口

~~~java

~~~

~~~java

~~~

~~~java

~~~



### 4. Mapper XML

~~~xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.buu.orderservice.mapper.OrderMainMapper">
    <!-- 复杂SQL在此处编写，基础CRUD由MyBatis-Plus提供 -->
</mapper>
~~~

~~~xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.buu.orderservice.mapper.OrderItemMapper">
    <!-- 复杂SQL在此处编写，基础CRUD由MyBatis-Plus提供 -->
</mapper>
~~~

~~~xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.buu.orderservice.mapper.AiChatRecordMapper">
    <!-- 复杂SQL在此处编写，基础CRUD由MyBatis-Plus提供 -->
</mapper>
~~~



### 5. Service 接口

**OrderMainService.java**



```java

```



~~~java

~~~

~~~java

~~~



### 6. Service 实现类

**OrderMainServiceImpl.java**



```java

```

~~~java

~~~



### 7. Controller

```java

```

### 8.统一返回

~~~java
package com.buu.orderservice.common;

import lombok.Data;

/**
 * 统一接口返回结果
 * 封装所有接口的响应格式，保证前后端交互规范统一
 * @param <T> 响应数据泛型
 */
@Data
public class R<T> {
    /** 响应状态码：200成功，500失败 */
    private Integer code;
    /** 响应提示信息 */
    private String msg;
    /** 响应业务数据 */
    private T data;

    /**
     * 成功响应（携带数据）
     * @param data 接口返回数据
     * @return 统一响应对象
     */
    public static <T> R<T> success(T data) {
        R<T> result = new R<>();
        result.setCode(200);
        result.setMsg("操作成功");
        result.setData(data);
        return result;
    }

    /**
     * 成功响应（无数据）
     * @return 统一响应对象
     */
    public static <T> R<T> success() {
        return success(null);
    }

    /**
     * 失败响应
     * @param message 错误描述信息
     * @return 统一响应对象
     */
    public static <T> R<T> fail(String message) {
        R<T> result = new R<>();
        result.setCode(500);
        result.setMsg(message);
        return result;
    }

    /**
     * 失败响应（自定义状态码）
     * @param code 错误状态码
     * @param message 错误描述信息
     * @return 统一响应对象
     */
    public static <T> R<T> fail(Integer code, String message) {
        R<T> result = new R<>();
        result.setCode(code);
        result.setMsg(message);
        return result;
    }
}
~~~



# 五、支付服务pay-service

## 1.依赖

~~~xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.buu</groupId>
        <artifactId>inventory-parent</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>pay-service</artifactId>
    <packaging>jar</packaging>
    <name>pay-service</name>
    <description>支付服务</description>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
~~~



## 2.配置

~~~properties
# ========== Basic Service Configuration ==========
# Service name for service identification
spring.application.name=pay-center
# Service listening port, consistent with the project specification
server.port=8085

# ========== Database Connection Configuration ==========
# MySQL JDBC driver class
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# JDBC connection URL for pay database, with time zone and encoding settings
spring.datasource.url=jdbc:mysql://localhost:3306/pay_db?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=GMT%2B8
# Database login username
spring.datasource.username=root
# Database login password
spring.datasource.password=root
# Maximum number of active connections in the connection pool
spring.datasource.hikari.maximum-pool-size=10
# Connection wait timeout in milliseconds
spring.datasource.hikari.connection-timeout=30000

# ========== MyBatis-Plus Configuration ==========
# Mapper XML file scan path
mybatis-plus.mapper-locations=classpath:mapper/**/*.xml
# Entity package path for type alias
mybatis-plus.type-aliases-package=com.buu.pay.entity
# Auto map underscore column name to camel case field
mybatis-plus.configuration.map-underscore-to-camel-case=true
# Disable second-level cache
mybatis-plus.configuration.cache-enabled=false
# Database primary key auto increment strategy
mybatis-plus.global-config.db-config.id-type=AUTO

# ========== Business Configuration ==========
# Payment request timeout in milliseconds
payment.request.timeout.ms=5000
# Number of retries for failed payment requests
payment.retry.count=3

# ========== Log Configuration ==========
# Mapper layer log level, print SQL for debugging
logging.level.com.buu.pay.mapper=debug
# Root log level
logging.level.root=info
~~~



## 3.代码

### 1. 启动



```java
package com.buu.payservice;

import org.mybatis.spring.annotation.MapperScan;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
@MapperScan("com.buu.payservice.mapper")
public class PayServiceApplication {

    public static void main(String[] args) {
        SpringApplication.run(PayServiceApplication.class, args);
    }

}

```

### 2. 实体类

```java
package com.buu.payservice.entity;

import com.baomidou.mybatisplus.annotation.IdType;
import com.baomidou.mybatisplus.annotation.TableId;
import com.baomidou.mybatisplus.annotation.TableName;
import lombok.Data;
import java.math.BigDecimal;
import java.time.LocalDateTime;

/**
 * 支付单实体
 * 对应 pay_db.payment 表
 */
@Data
@TableName("payment")
public class Payment {
    /** 支付单主键ID */
    @TableId(type = IdType.AUTO)
    private Long payId;
    /** 支付单号 */
    private String payNo;
    /** 关联订单编号 */
    private String orderNo;
    /** 支付用户ID */
    private Long userId;
    /** 支付金额 */
    private BigDecimal payAmount;
    /** 支付方式 */
    private String payMethod;
    /** 支付状态：待支付、支付成功、支付失败 */
    private String payStatus;
    /** 支付回调报文内容 */
    private String callbackContent;
    /** 支付回调时间 */
    private LocalDateTime callbackTime;
    /** 创建时间 */
    private LocalDateTime createTime;
    /** 更新时间 */
    private LocalDateTime updateTime;
}
```

### 3. Mapper 接口

```java

```

### 4. Service 接口与实现



```java

```



```java

```

### 5. Controller



```

```

### 6. 统一返回

~~~java
package com.buu.payservice.common;

import lombok.Data;

/**
 * 统一接口返回结果
 * 封装所有接口的响应格式，保证前后端交互规范统一
 * @param <T> 响应数据泛型
 */
@Data
public class R<T> {
    /** 响应状态码：200成功，500失败 */
    private Integer code;
    /** 响应提示信息 */
    private String msg;
    /** 响应业务数据 */
    private T data;

    /**
     * 成功响应（携带数据）
     * @param data 接口返回数据
     * @return 统一响应对象
     */
    public static <T> R<T> success(T data) {
        R<T> result = new R<>();
        result.setCode(200);
        result.setMsg("操作成功");
        result.setData(data);
        return result;
    }

    /**
     * 成功响应（无数据）
     * @return 统一响应对象
     */
    public static <T> R<T> success() {
        return success(null);
    }

    /**
     * 失败响应
     * @param message 错误描述信息
     * @return 统一响应对象
     */
    public static <T> R<T> fail(String message) {
        R<T> result = new R<>();
        result.setCode(500);
        result.setMsg(message);
        return result;
    }

    /**
     * 失败响应（自定义状态码）
     * @param code 错误状态码
     * @param message 错误描述信息
     * @return 统一响应对象
     */
    public static <T> R<T> fail(Integer code, String message) {
        R<T> result = new R<>();
        result.setCode(code);
        result.setMsg(message);
        return result;
    }
}
~~~

# 六、父项目依赖

~~~xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.12</version>
        <relativePath/>
    </parent>

    <groupId>com.buu</groupId>
    <artifactId>inventory-parent</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>
    <name>inventory-parent</name>

    <modules>
        <module>user-service</module>
        <module>product-service</module>
        <module>stock-service</module>
        <module>order-service</module>
        <module>pay-service</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <mybatis-plus.version>3.5.5</mybatis-plus.version>
        <mybatis-spring.version>3.0.3</mybatis-spring.version>
        <mysql.version>8.0.33</mysql.version>
        <maven.compiler.version>3.11.0</maven.compiler.version>
    </properties>

    <dependencyManagement>
        <dependencies>

            <!-- MyBatis-Plus -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-boot-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <dependency>
                <groupId>org.mybatis</groupId>
                <artifactId>mybatis-spring</artifactId>
                <version>${mybatis-spring.version}</version>
            </dependency>

            <!-- MySQL 驱动 -->
            <dependency>
                <groupId>com.mysql</groupId>
                <artifactId>mysql-connector-j</artifactId>
                <version>${mysql.version}</version>
            </dependency>

            <!-- 注意：spring-boot-starter-*、lombok、spring-security-crypto 等版本
                 由 spring-boot-starter-parent 管理，此处不要再声明！ -->
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <!-- 版本由 parent 管理 -->
                    <configuration>
                        <excludes>
                            <exclude>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                            </exclude>
                        </excludes>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven.compiler.version}</version>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                        <encoding>${project.build.sourceEncoding}</encoding>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
~~~

